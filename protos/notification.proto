syntax = "proto3";

package ceramicraft.notification;

option go_package = "github.com/sw5005-sus/ceramicraft-push-client/pb;pb";

message SendUserPushRequest {
    int64 user_id = 1;
    string title = 2;
    string body = 3;
    map<string, string> data = 4;
}

message SendUserPushResponse {
    bool success = 1;
    int32 sent_count = 2;
    repeated string failed_tokens = 3;
}

service NotificationService {
    rpc SendUserPush (SendUserPushRequest) returns (SendUserPushResponse);
}
